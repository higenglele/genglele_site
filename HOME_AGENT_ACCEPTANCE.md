# 第三步：场景计划与确认执行

## 页面联调记录（2026-09-06）

- 实际输入聚焦时已移除绿色外框。
- 使用真实模型请求客厅灯亮度40%，生成确认计划；确认后结果与设备页亮度均为40%，显示蓝色“已调整”信号。
- 从对话切换设备页再返回，保留已完成对话。
- 冷却期内触发相同睡眠事件，显示5分钟内不重复提醒，未发起新计划。

## 第五步：主动建议

在 Agent 对话展开“体验主动建议”，点击“模拟回家”或“模拟到睡眠时间”。这是手动模拟的环境信号，没有真实定位、后台监听或定时器触发。

- Agent 读取设备和偏好后判断是否需要调整；需要时显示待确认计划，不需要时说明保持现状。
- 确认前设备不变；取消不执行；确认后只显示简短结果。
- 同类事件5分钟内再次点击只提示已提醒，不再请求模型。不同事件单独计时，处理中不接收新事件。
- 请求失败且尚未提出计划时可立即重试；事件不允许保存偏好。

启动：`npm run dev -- --host 127.0.0.1`，打开 `http://127.0.0.1:5173/#home-agent` 的 Agent 对话。

模型调用需要本机 `.env.local` 中的 DeepSeek 配置。操作对象均为浏览器内的模拟设备。

## 验收

1. 输入“我要准备睡觉了，请安排设备”：Agent 查询设备，提出带理由和具体设备参数的计划，等待确认。此时设备不变。
2. 点击“取消计划”：记录取消，不执行设备调整，也不会自行发起第二份计划。
3. 重新发出指令，点击“确认执行”：逐项显示成功或失败；设备管理页的状态与结果一致，刷新后保留。
4. 在设备管理中将床头灯设为离线，再要求“把客厅灯和床头灯打开”：若计划包含离线床头灯，其执行失败，不能报全成功；客厅灯独立执行。
5. 输入“卧室灯调到三成亮并打开”：单设备修改也需要确认；纯查询不需要确认。
6. 切换家庭概览、设备管理、Agent 对话：保留对话、输入及待确认计划，切换本身不执行。期间手动修改设备后，旧计划确认时应提示重新生成。
7. 刷新或返回个人网站后重新进入：恢复本浏览器最近24小时已完成的对话；每轮从用户发起时计时，后续聊天不续期。界面定期清理过期轮次，请求模型前再次过滤。未完成计划不自动恢复或执行，需要重新发出指令。无时间戳的旧会话不迁移。
8. 操作完成只显示简短设备结果，隐藏工具日志及带工具调用的过程消息；开始对话后不再显示快捷示例按钮。

自动化验证：`npm test`（参数校验、取消、一次确认、状态变化、离线与部分失败），`npm run build`。

## 第四步：明确偏好记忆

1. 输入“记住：睡觉时空调设为26度”：应出现保存确认，点击记住后在记忆栏可见，设备不改变。取消则不保存。
2. 刷新后偏好仍可见；修改为25度并保存，再发出睡眠场景请求，Agent 应读取最新偏好提出计划。
3. 删除偏好后再次提出场景请求，Agent 不应把旧对话中的该偏好当作当前记忆。
4. 临时指令不应自动保存偏好。纯保存请求不应调整设备。

对话和上下文保留24小时；明确保存的偏好保留到修改或删除。均存于当前浏览器，尚无跨设备同步。未打开应用时不运行清理任务，下次打开时清理过期记录。

当前没有定时自动化或真实硬件接入。确认边界位于模拟设备执行端；接入真实硬件时需要在服务端实现身份、权限和计划授权。
