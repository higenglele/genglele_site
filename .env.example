# Copy to .env.local. Never put keys in a VITE_ variable.
DEEPSEEK_API_KEY=
DEEPSEEK_MODEL=deepseek-v4-flash

# Explicitly use non-thinking mode for low-latency single-device calls.
DEEPSEEK_THINKING=disabled
