# 网站维护入口

涉及本仓库的网站内容、项目卡片或部署时，先阅读 [网站维护与项目排版规范](docs/网站维护与项目排版规范.md)。该文档保存文件索引、项目映射、用户确认的交互、检查和发布流程。

- 正式网站为 `https://genglele.com/`，仓库为 `higenglele/genglele_site`，生产分支 `main`，通过现有Vercel集成发布。
- 项目文案在 `src/App.tsx` 的 `projectCards`，共性卡片结构在 `ProjectCard`，悬停样式在 `src/index.css`。
- 三个项目入口全部新标签页打开并保留首页；不要恢复哈希链接当前页打开的例外。
- 不恢复卡片点击后的黄色/橙色焦点光圈。保留链接键盘操作能力。
- 私信打开静默已读，列表切换/回复保持滚动位置；不要恢复已读弹窗或自动聚焦回复框。
- 登录授权使用外部默认浏览器。Safari验收已取消，除非用户重新要求。
- 修改前核对工作树和远端，保留无关修改。发布授权根据当前任务及会话判断，本文本身不授权任意发布。
- 测试入口 `npm test`，构建入口 `npm run build`；浏览器回归脚本及启动条件见维护文档。
- 更改结构、文件位置、跳转规则或部署方式时，同步更新维护文档。
